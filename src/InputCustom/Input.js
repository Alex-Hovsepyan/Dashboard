import "./InputCustom";
import "./input"

function Input ({color,type, placeholder, value, backgroundcolor,width,height}) {
    return (
        <input
        displyay= {displyay}
        type={type}
        placeholder={placeholder}
        value={value}
        style={{width:width,
                height:height,

            
            
            }}
        />
        
    )
}

export default Input